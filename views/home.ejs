<%- include("header") %>

  <main class="px-3">
    <h1>Ready to report this nuisance 1 pothole at a time?</h1>
    <p class="lead">We need your location to find the details of your local authorities to contact. Please click allow to move ahead.</p>
    <p class="lead">
      <!-- <button class="btn btn-lg btn-secondary fw-bold border-white bg-white">Let's Begin!!!</button> -->
    
      <form method="POST" id="geoloc">
        <input id="lat" type="hidden" name="lat" value="0"/>
        <input id="long" type="hidden" name="long" value="0"/>
        <!-- <button type="submit" class="btn btn-lg btn-secondary fw-bold border-white bg-white" disabled>Let's Begin!!!</button> -->
        <button id="sub" type="submit" class="btn btn-lg btn-light fw-bold" disabled>Allow Access</button>
      </form>
    </p>
  </main>

  <footer class="mt-auto text-white-50">
    <p>Made with ♥ by <a href="https://github.com/KR2206" class="text-white" target="_blank">KR2206</a>.</p>
  </footer>
</div>

    <script>
    getLocation();    
    function getLocation() {
        if (navigator.geolocation) {            
            navigator.geolocation.getCurrentPosition(updatePos);
        }
    }

    function updatePos(position) {
        document.getElementById("sub").innerText="Let's Begin!!!";
        document.getElementById("sub").disabled=false;
        document.getElementById("lat").value = position.coords.latitude;
        document.getElementById("long").value = position.coords.longitude;

        console.log(position.coords.latitude);
        console.log(position.coords.longitude);
    }

    </script>

    
<%- include("footer") %>